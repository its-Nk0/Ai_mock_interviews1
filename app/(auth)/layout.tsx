import {ReactNode}from 'react'

const Aultlayout = ({children}:{children:ReactNode}) => {
  return (
    <div className="auth-Layout">
      {children}
    </div>
  )
}

export default Aultlayout
